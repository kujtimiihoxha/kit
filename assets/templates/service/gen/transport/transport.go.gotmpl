package transport

{{ $projectModule := "" }}
{{ with .Config }}
	{{ $projectModule = .Module }}
{{ end }}

import 	"{{ $projectModule }}/{{ .Package }}/gen/transport/http"

type Transports interface {
	HTTP() http.Transport
}
